'use strict';

describe('coffee_shop.detail module', function() {

  beforeEach(module('coffee_shop.detail'));

  describe('detail controller', function(){

    it('should ...', inject(function($controller) {
      var detailCtrl = $controller('DetailCtrl');
      expect(detailCtrl).toBeDefined();
    }));

  });
});
